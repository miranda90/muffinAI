# Deprecado

Contenido retirado el 2026-08-28: recomendaba el "formato simplificado" con atributos
legacy (`padding_top`, `bg_color`), que el validador marca como error (E021) porque el
front los vuelca como CSS inline.

Fuente de verdad: `.claude/skills/bebuilder-json/SKILL.md` + `docs/bebuilder/`.
